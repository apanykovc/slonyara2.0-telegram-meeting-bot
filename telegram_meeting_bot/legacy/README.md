# Legacy Code

This directory stores legacy PTB handler snippets kept only for reference.
They are not imported by the running aiogram application.

Current runtime source of truth:
- `telegram_meeting_bot/aiogram_app/app.py`

Do not patch files under `legacy/` for production fixes unless explicitly migrating legacy behavior.
